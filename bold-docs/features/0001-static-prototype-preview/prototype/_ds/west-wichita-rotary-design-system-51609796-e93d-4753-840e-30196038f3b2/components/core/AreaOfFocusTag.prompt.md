Tags a service project by one of Rotary's seven official Areas of Focus, colored to match the official icon set.

```jsx
<AreaOfFocusTag area="water" />
<AreaOfFocusTag area="education" />
```

Valid `area` values: `peace`, `disease`, `water`, `economic`, `environment`, `education`, `maternal`.
